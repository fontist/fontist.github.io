---
import DefaultLayout from '../../layouts/DefaultLayout.astro'
import PropertyListPage from '../../islands/PropertyListPage.vue'

const property = 'scripts'
const pageTitle = 'Unicode Scripts — Fontist'
const propTitle = 'Scripts'
const label = 'scripts'
---

<DefaultLayout title={pageTitle}>
  <PropertyListPage property={property} title={propTitle} label={label} client:load />
</DefaultLayout>
