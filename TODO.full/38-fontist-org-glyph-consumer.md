# 38 — fontist.org glyph consumer

Glyph detail page + panglyph fallback. REQ R8 + R9.

## Deliverable

- `/unicode/glyph/{hex}` route: large SVG, full outline path data,
  provenance chain
- panglyph WOFF2 fallback injected as lowest-priority `@font-face`

## Status

Stub — full plan to follow. Referenced by `REQ-unicode-browser-and-archive-integration.md` R8–R9.

## Dependencies

- TODO.full/10 (universal glyph set rendering)
- panglyph repo (out of scope here)
