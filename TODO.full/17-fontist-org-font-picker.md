# 17 — fontist.org font picker

Sticky font picker on `/unicode` that persists via Pinia + localStorage.
REQ R4.

## Deliverable

- Extract `injectFontFace` to a global composable
- `<FontPicker>` component on `/unicode`
- Pinia store + localStorage persistence
- Block list shows coverage bars next to each block name when a font is active

## Status

Stub — full plan to follow. Referenced by `REQ-unicode-browser-and-archive-integration.md` R4.

## Dependencies

- TODO.full/10 (WOFF injection)
- TODO.full/11 (coverage bars)
