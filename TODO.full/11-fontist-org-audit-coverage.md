# 11 — fontist.org audit coverage UI

Per-block and per-codepoint coverage indicators from the audit JSONs.
REQ R5 + R6 + R7.

## Deliverable

- Per-block coverage bar (parse audit JSON, render fill-ratio bar)
- Per-codepoint covered/missing indicator on the block grid
- `/unicode/best-fonts/{block}` view: fonts sorted by fill ratio

## Status

Stub — full plan to follow. Referenced by `REQ-unicode-browser-and-archive-integration.md` R5–R7.

## Dependencies

- TODO.full/09 (audit JSON is in archive-public)
